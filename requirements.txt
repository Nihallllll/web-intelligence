# Core (always required)
httpx[http2]
beautifulsoup4
trafilatura
numpy

# Embeddings — pick ONE:
# sentence-transformers   # GPU-accelerated (~2 GB)
# torch                   # required by sentence-transformers
# fastembed               # lightweight ONNX (~200 MB)

# Vector store (optional — falls back to NumPy if missing)
# chromadb

# Web search (optional — enables pipeline.search_web())
# duckduckgo-search

# REST API server (optional — enables web_intelligence.server)
# fastapi
# uvicorn

# Jupyter / async loop compat (optional)
# nest_asyncio

# Dev
# python-dotenv
# pytest
# ruff